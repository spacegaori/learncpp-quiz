#include <iostream>

/*
1. Declare an array to hold the high temperature 
(to the nearest tenth of a degree) for each day 
of a year (assume 365 days in a year). Initialize 
the array with a value of 0.0 for each day.
*/

int main()
{
    double temperature[365] { };

    std::cout << temperature[0] << '\n';
}